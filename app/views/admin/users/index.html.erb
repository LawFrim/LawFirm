<ul class="breadcrumb">
  <li class="active">
      <%= link_to("所有用户", admin_users_path) %>
  </li>
  <li>
    <%= link_to("所有会员", admin_users_path(:order => "by_account")) %>
  </li>
  <li>
    <%= link_to("所有律师", admin_users_path(:order => "by_lawyer")) %>
  </li>
</ul>
<table class="table table-bordered">
  <thead>
      <tr>
        <td>用户ID</td>
          <td>用户名</td>
          <td>注册时间</td>
          <td>上次登录时间</td>
          <td>管理选项</td>
          <td>身份认证</td>
          <td>律师声誉</td>
          <td>支付状况</td>
      </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <%= user.id %>
        </td>
        <td>
            <%= render_user_status(user) %>
          <%= user.user_name %>
        </td>
        <td>
            <%= user.created_at %>
        </td>
        <td>
            <%= user.last_sign_in_at %>
        </td>
        <td>
          <!-- <%= link_to("Edit", edit_admin_user_path(user))%>  -->
            <%= change_user_status(user) %>|
            <%= link_to("删除用户", admin_user_path(user),
                        method: :delete, data: { confirm: "Are you sure?" },class:"btn btn-xs btn-info")  %>

        </td>
        <td>
          待添加
        </td>
        <td>
          待添加
        </td>
        <td>
          待添加
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to("修改密码",edit_user_registration_path, class: "btn btn-sm btn-default" ) %>
